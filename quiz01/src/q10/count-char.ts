export{};
let args = process.argv.slice(2);
let len = args.length;
let fac = 1;
// if(n <= 0 || isNaN(n) || !Number.isInteger(n)){
//     console.log("Invalid Input");
//     process.exit();
// }
for(let i = 0;i<len;i++){
    
}